// Where to save the project configuration file
export const projectConfigPath = 'project.json'

// WARN: MSVC Install Path must not have spaces in it,as they will break the build
export const MSVCInstallDir = "C:\\MicrosoftVisualStudio"
